[package]
name = "accrual-engine-wasm"
version = "0.1.0"
edition = "2021"
description = "ESTA Tracker accrual engine compiled to WASM"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
proptest = "1.4"
