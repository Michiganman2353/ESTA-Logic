{
  "name": "@esta-logic/helix",
  "version": "1.0.0",
  "private": true,
  "main": "gleam.toml",
  "description": "Pure functional Gleam accrual engine - compiles to WASM for Tauri pivot",
  "scripts": {
    "build": "gleam deps download --all && gleam build",
    "test": "gleam deps download --all && gleam test",
    "test:coverage": "gleam deps download --all && gleam test"
  }
}
