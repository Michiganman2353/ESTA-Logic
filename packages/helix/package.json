{
  "name": "@esta-logic/helix",
  "version": "1.0.0",
  "private": true,
  "main": "gleam.toml",
  "description": "Pure functional Gleam accrual engine - compiles to WASM for Tauri pivot",
  "scripts": {
    "build": "gleam build",
    "test": "gleam deps download && gleam test",
    "test:coverage": "gleam deps download && gleam test"
  }
}
