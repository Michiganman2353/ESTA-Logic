# Upstream harmony – sync with master to prevent diverge shadows
git pull --rebase origin master || {
    echo "Rebase failed - please resolve conflicts before pushing"
    exit 1
}

# Lint as guard – pure before push
nx affected --target=lint --base=origin/master || {
    echo "Linting failed - please fix lint errors before pushing"
    exit 1
}
